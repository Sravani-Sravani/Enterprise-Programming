//12 else if ladder
        var a,b,c;
    a=parseInt(prompt("Enter first value",0))
    b=parseInt(prompt("Enter second value",0))
    c=parseInt(prompt("Enter third value",0))
    if (a>=b&&a>=c)
          alert(a+" is big")
        else if (b>=a&&b>=c)
          alert(b+" is big")
        else
            alert(c+" is big")
